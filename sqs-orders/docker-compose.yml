services:
  elasticmq:
    image: softwaremill/elasticmq-native:1.6.11
    ports:
      - "9324:9324"
    volumes:
      # Pre-creates the `orders` queue (see elasticmq.conf).
      - ./elasticmq.conf:/opt/elasticmq.conf:ro
