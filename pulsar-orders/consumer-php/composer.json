{
    "name": "babelqueue/pulsar-orders-consumer-php",
    "description": "PHP consumes canonical BabelQueue envelopes from Apache Pulsar over the native WebSocket consumer API (§5 PHP consume path).",
    "require": {
        "php": ">=8.2",
        "babelqueue/php-sdk": "^1.5",
        "textalk/websocket": "^1.6"
    },
    "autoload": { "psr-4": { "App\\": "src/" } },
    "config": { "sort-packages": true }
}
