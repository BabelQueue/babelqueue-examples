{
    "name": "babelqueue/kafka-orders-consumer-php",
    "description": "PHP consumes canonical BabelQueue envelopes from Apache Kafka over ext-rdkafka (§6 PHP consume path, process-then-commit).",
    "require": {
        "php": ">=8.2",
        "ext-rdkafka": "*",
        "babelqueue/php-sdk": "^1.6"
    },
    "autoload": { "psr-4": { "App\\": "src/" } },
    "config": { "sort-packages": true }
}
